services:
  api:
    build: ./app
    container_name: femquest-orchestrator-api
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - orchestrator_data:/data

  caddy:
    image: caddy:2
    container_name: femquest-orchestrator-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api

volumes:
  orchestrator_data:
  caddy_data:
  caddy_config: